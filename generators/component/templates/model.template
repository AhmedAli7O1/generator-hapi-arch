'use strict';

const mongoose = require('mongoose');
<% if (isSub) { %>
module.exports = new mongoose.Schema({
  name: String,
  age: Number
});
<% } else { %>
module.exports = new mongoose.Schema({});
<% } %>
